# Mobile app environment configuration
# The API base URL is set in src/api/client.ts (BASE_URL constant).
# Production APK uses the deployed backend below.
# For local development, change BASE_URL in src/api/client.ts to your machine's IP.

API_BASE_URL=https://<your-deployed-backend-url>
